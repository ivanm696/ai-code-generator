import React from 'react'
import ReactDOM from 'react-dom/client'
import BoltClone from './components/BoltClone'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BoltClone />
  </React.StrictMode>
)
